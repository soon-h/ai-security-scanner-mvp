# Claude(Anthropic) API 키. 미설정 시 Claude 분석은 stub로 대체된다(점검 결과는 유지).
ANTHROPIC_API_KEY=

# 사용할 Claude 모델 (기본: claude-sonnet-5)
ANTHROPIC_MODEL=claude-sonnet-5

# 런타임 executor 강제 선택: auto | docker | stub (기본 auto)
# auto: docker 가용 시 docker, 아니면 stub
AIRPOD_EXECUTOR=auto
